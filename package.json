{
  "name": "landing-page",
  "version": "1.0.0",
  "description": "A product landing page npm demo",
  "main": "index.js",
  "scripts": {
    "index": "npx sass ./src/style.scss ./public/style.css",
    "index-watch": "npx sass ./src/style.scss ./public/style.css --watch",

    "action": "npx sass ./src/action-style.scss ./public/action-style.css",
    "action-watch": "npx sass ./src/action-style.scss ./public/action-style.css --watch",
    
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "landing",
    "page"
  ],
  "author": "John Paul Bocatija",
  "license": "MIT",
  "devDependencies": {
    "sass": "^1.50.1"
  }
}
